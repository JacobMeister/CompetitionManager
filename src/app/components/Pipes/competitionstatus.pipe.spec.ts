import { CompetitionstatusPipe } from './competitionstatus.pipe';

describe('CompetitionstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CompetitionstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
