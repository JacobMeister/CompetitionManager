import { GamestatusPipe } from './gamestatus.pipe';

describe('GamestatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GamestatusPipe();
    expect(pipe).toBeTruthy();
  });
});
